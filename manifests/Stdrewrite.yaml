# GENERATED by ccw-manifest — DO NOT EDIT.
# Source of truth: the rule declarations in each listed ruleset directory.
# Regenerate: ccw-manifest ; verify: ccw-manifest --check
generator: ccw-manifest/0.1
glue_abi: 1
rule_format: 1
total_rules: 307
rulesets:
  - path: rewrite-salvo/arith/identity/
    name: arith.identity
    description: Additive and multiplicative identities (x+0, x*1, x-0, 0-x, x-x).
    semantics: equivalence
    side_conditions: false
    rules: 18
    profiles: [core]
  - path: rewrite-salvo/arith/constant-fold/
    name: arith.constant-fold
    description: Folds integer add/sub/mul/neg on literal operands.
    semantics: equivalence
    side_conditions: false
    rules: 16
    profiles: [core]
  - path: rewrite-salvo/arith/reassociate/
    name: arith.reassociate
    description: Reassociates commutative integer chains to group constants.
    semantics: equivalence
    side_conditions: false
    rules: 12
    profiles: [core]
  - path: rewrite-salvo/arith/strength-reduction/
    name: arith.strength-reduction
    description: Multiplies and divides by powers of two into shifts; small-constant multiply into shift-add.
    semantics: equivalence
    side_conditions: true
    rules: 14
    profiles: [core]
  - path: rewrite-salvo/arith/distribute/
    name: arith.distribute
    description: Distribution and factoring of multiplication over addition.
    semantics: equivalence
    side_conditions: false
    rules: 10
    profiles: [core]
  - path: rewrite-salvo/arith/negation/
    name: arith.negation
    description: Double negation, negation of subtraction, and sign-flip normalization.
    semantics: equivalence
    side_conditions: false
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/arith/overflow-guarded/
    name: arith.overflow-guarded
    description: Rewrites valid only under proven no-overflow; guarded by value-range side conditions.
    semantics: equivalence
    side_conditions: true
    rules: 12
    profiles: [core]
  - path: rewrite-salvo/bitwise/identity/
    name: bitwise.identity
    description: And/or/xor identities with 0, all-ones, and self.
    semantics: equivalence
    side_conditions: false
    rules: 16
    profiles: [core]
  - path: rewrite-salvo/bitwise/constant-fold/
    name: bitwise.constant-fold
    description: Folds and/or/xor/not on literal operands.
    semantics: equivalence
    side_conditions: false
    rules: 12
    profiles: [core]
  - path: rewrite-salvo/bitwise/demorgan/
    name: bitwise.demorgan
    description: De Morgan dualities between and/or under complement.
    semantics: equivalence
    side_conditions: false
    rules: 6
    profiles: [core]
  - path: rewrite-salvo/bitwise/shift/
    name: bitwise.shift
    description: Shift combining, shift-by-zero, and shift-of-constant; guarded by shift-amount range conditions.
    semantics: equivalence
    side_conditions: true
    rules: 14
    profiles: [core]
  - path: rewrite-salvo/bitwise/mask/
    name: bitwise.mask
    description: Mask composition, redundant-mask removal, and mask-then-shift reordering.
    semantics: equivalence
    side_conditions: true
    rules: 10
    profiles: [core]
  - path: rewrite-salvo/power/
    name: power.consumption
    description: Low-toggle bitwise identities and small integer strength reductions for lower-work implementations.
    semantics: equivalence
    side_conditions: false
    rules: 14
    profiles: [core]
  - path: rewrite-salvo/cmp/canonical/
    name: cmp.canonical
    description: Comparison polarity and operand-order normalization (gt to lt-swapped, ge to not-lt).
    semantics: equivalence
    side_conditions: false
    rules: 12
    profiles: [core]
  - path: rewrite-salvo/cmp/constant-fold/
    name: cmp.constant-fold
    description: Folds comparisons of literal operands to boolean constants.
    semantics: equivalence
    side_conditions: false
    rules: 10
    profiles: [core]
  - path: rewrite-salvo/cmp/range/
    name: cmp.range
    description: Decides comparisons from value-range facts; guarded by range side conditions.
    semantics: equivalence
    side_conditions: true
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/bool/simplify/
    name: bool.simplify
    description: Boolean absorption, idempotence, complement, and constant rules.
    semantics: equivalence
    side_conditions: false
    rules: 12
    profiles: [core]
  - path: rewrite-salvo/bool/branch-select/
    name: bool.branch-select
    description: Converts branch-on-constant-condition and same-target diamonds into selects.
    semantics: equivalence
    side_conditions: false
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/select/simplify/
    name: select.simplify
    description: Select with equal arms, constant condition, or negated condition.
    semantics: equivalence
    side_conditions: false
    rules: 10
    profiles: [core]
  - path: rewrite-salvo/divmod/power-of-two/
    name: divmod.power-of-two
    description: Unsigned div/mod by powers of two into shift/mask; guarded by power-of-two side conditions.
    semantics: equivalence
    side_conditions: true
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/divmod/identity/
    name: divmod.identity
    description: Division and remainder identities (x/1, x%1, x/x); guarded by nonzero-divisor conditions.
    semantics: equivalence
    side_conditions: true
    rules: 6
    profiles: [core]
  - path: rewrite-salvo/float/identity-safe/
    name: float.identity-safe
    description: Float identities valid under IEEE-754 without fast-math assumptions (x*1.0, negate-negate).
    semantics: equivalence
    side_conditions: true
    rules: 6
    profiles: [core]
  - path: rewrite-salvo/float/constant-fold/
    name: float.constant-fold
    description: Folds float arithmetic on literals with round-to-nearest-even semantics.
    semantics: equivalence
    side_conditions: false
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/cast/simplify/
    name: cast.simplify
    description: Eliminates redundant widen-narrow pairs and same-type casts; guarded by width conditions.
    semantics: equivalence
    side_conditions: true
    rules: 12
    profiles: [core]
  - path: rewrite-salvo/cast/constant-fold/
    name: cast.constant-fold
    description: Folds casts of literal operands.
    semantics: equivalence
    side_conditions: false
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/norm/commutative-order/
    name: norm.commutative-order
    description: Orders commutative operands by canonical node ordering.
    semantics: equivalence
    side_conditions: false
    rules: 6
    profiles: [core]
  - path: rewrite-salvo/norm/constant-right/
    name: norm.constant-right
    description: Moves literal operands to the right of commutative operations.
    semantics: equivalence
    side_conditions: false
    rules: 4
    profiles: [core]
  - path: rewrite-salvo/norm/compare-polarity/
    name: norm.compare-polarity
    description: Normalizes negated comparisons to positive polarity with swapped branches.
    semantics: equivalence
    side_conditions: false
    rules: 5
    profiles: [core]
  - path: rewrite-salvo/mem/load-store/
    name: mem.load-store
    description: Store-to-load forwarding and redundant-load elimination; guarded by no-alias side conditions.
    semantics: equivalence
    side_conditions: true
    rules: 8
    profiles: [core]
  - path: rewrite-salvo/peephole/window/
    name: peephole.window
    description: Adjacent-instruction pair rewrites (add-then-sub of same constant, move chains).
    semantics: equivalence
    side_conditions: false
    rules: 14
    profiles: [core]
